import * as React from 'react';
import UserCardList from 'src/components/common/UserCardList';

class UserFollowingCards extends React.Component<{}> {
  public render() {
    return <UserCardList />;
  }
}

export default UserFollowingCards;
