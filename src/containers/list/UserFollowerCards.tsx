import * as React from 'react';
import UserCardList from 'src/components/common/UserCardList';

class UserFollowerCards extends React.Component<{}> {
  public render() {
    return <UserCardList />;
  }
}

export default UserFollowerCards;
