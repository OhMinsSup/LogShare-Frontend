import * as React from 'react';
import AuthForm from 'src/components/landing/AuthForm';

type Props = {};

class AuthFormContainer extends React.Component<Props> {
  public render() {
    return <AuthForm />;
  }
}

export default AuthFormContainer;
